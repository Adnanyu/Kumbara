{
  "name": "kumbara",
  "private": true,
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "heroku-postbuild": "npm install --prefix client --legacy-peer-deps && npm run build --prefix client && npm install --prefix server --legacy-peer-deps && npm run build --prefix server",
    "start": "node server/dist/index.js"
  }
}